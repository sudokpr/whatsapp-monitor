[project]
name = "whatsapp-group-monitor"
version = "0.1.0"
description = "WhatsApp monitor with Python digest sidecar."
requires-python = ">=3.11"
dependencies = [
  "dspy>=2.6",
  "openai-codex>=0.1.0b3",
  "requests>=2.32",
]

[tool.uv]
package = false
